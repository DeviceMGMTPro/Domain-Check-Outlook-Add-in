// Example UI logic for displaying messages or handling button clicks in the index.html
document.addEventListener("DOMContentLoaded", function () {
    const resultDiv = document.getElementById('result');

    // Example of displaying a simple message in the UI
    resultDiv.innerText = "This is the Domain Check Add-in. The functionality works in the background when sending emails.";
});
